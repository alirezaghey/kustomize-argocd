apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: second-test-nginx-argo-application
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/alirezaghey/kustomize-argocd.git
    targetRevision: HEAD
    path: overlays/second-test
  destination:
    server: https://kubernetes.default.svc
    namespace: nginx-second-test

  syncPolicy:
    syncOptions:
      - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true
